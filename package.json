{
  "name": "causeway-sh-sdk-ts",
  "private": true,
  "version": "0.1.0-alpha.0",
  "description": "Causeway TypeScript SDK monorepo. Seven packages: core + tenant + evm + zec + btc + sapling + orchard.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "proto:gen": "buf generate"
  },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "@bufbuild/buf": "^1.47.2",
    "@bufbuild/protoc-gen-es": "^2.2.2",
    "typescript": "^5.6.0",
    "vitest": "^2.1.4"
  }
}
